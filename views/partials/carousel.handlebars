<div class="carousel carousel-slider">
    <div class="carousel-item" >
    <a href="#one!"><img src="{{games.0.thumbnail}}"></a>
    <p>{{games.0.short_description}}</p>
    </div>
    <div class="carousel-item">
    <a  href="#two!"><img src="{{games.1.thumbnail}}"></a>
    <p>{{games.1.short_description}}</p>
    </div>
    <div class="carousel-item">
    <a  href="#three!"><img src="{{games.2.thumbnail}}"></a>
    {{games.2.short_description}}
    </div>
    <div class="carousel-item">
    <a  href="#four!"><img src="{{games.3.thumbnail}}"></a>
    <p>{{games.3.short_description}}</p>
    </div>
  </div>







